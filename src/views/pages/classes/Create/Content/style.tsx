import {createStyles} from '@mantine/core';

const useStyle = createStyles<string, {}>(() => ({
  root: {
    // height: 'calc(100vh - 282px)',
    backgroundColor: '#fff',
    marginTop: 12,
    padding: '0px 12px 12px 12px',
  },

  //Style for Student UI
}));

export default useStyle;
